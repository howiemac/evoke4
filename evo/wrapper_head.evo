# standard wrapper head
head:
  # CSS and JS imports
  wrapper_head_resources.evo  
  #  hook for apps to add webfonts
  wrapper_head_webfonts.evo
  # title
  title:req.user.get_HTML_title(self,req)
  # meta data
  wrapper_head_meta.evo
